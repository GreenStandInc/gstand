# gstand

To install dependencies:

```bash
npm install
```

To run:

```bash
npx tsx watch src/index.ts
```
To compile (without running):

```bash
npx tsup
```

To run (compiled js file):

```bash
node dist/index.cjs
```
